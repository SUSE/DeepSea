// -----------------------------------------------------------------------------
// ds-deploy.js
//
// The beginnings of a cluster deployment element.
// -----------------------------------------------------------------------------

Polymer({
    is: "ds-deploy",

    properties: {
    },

    ready: function() {
        console.log(this.is + ": ready.");
    }
});
