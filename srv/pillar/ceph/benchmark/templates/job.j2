[global]
directory={{ dir }}
filename_format=seqential.$filenum
wait_for_previous
filesize={{ filesize }}
blocksize={{ blocksize }}
numjobs={{ number_of_workers}}

[{{ number_of_workers}}w_{{ blocksize }}_{{ operation }}]
rw={{ operation }}
