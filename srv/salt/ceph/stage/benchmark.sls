
{# This is the "everything is working correctly" benchmark.  General idea #}
{# is to benchmark each osd.  Display any that fail completely.  Display #}
{# any outliers. #}

{# We don't really want this running everytime.  Still would want it to run #}
{# if invoked manually without complication #}
